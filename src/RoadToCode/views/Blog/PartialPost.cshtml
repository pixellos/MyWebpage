@using RoadToCode.Models.Blog
@model CreatePostViewModel

<div class="card-panel hoverable">
    <div class="card-panel flex-text">
        <div class="card-panel">
            <div class="row">
                <div class="col s4 img-responsive">
                    <img src="@Model.Picture"/>
                </div>
                <h3 class="col s5">@Html.Raw(Model.Title)</h3>
                <h6 class="col s3">przez <a href="#">Pixela</a> <small>@Model.DateTime</small></h6>
            </div>
        </div>
        <div class="markdown">
@Model.Content
        </div>
    </div>
</div>